[build]
builder = "RAILPACK"
buildCommand = "bun install && bun run build:ai && bun run build:api"

[deploy]
startCommand = "node api/dist/main"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
