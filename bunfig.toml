[workspace]
# Bun workspace configuration
members = [
  "our-app",
  "api", 
  "packages/*"
]

[install]
# Force all dependencies to root node_modules (no individual package node_modules)
hoistingLimits = "workspaces"
