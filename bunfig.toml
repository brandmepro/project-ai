[workspace]
# Bun workspace configuration
members = [
  "our-app",
  "api", 
  "packages/*"
]

[install]
# Revert to default isolated linker for stability
# We will handle single instances via webpack aliases
# linker = "hoisted"

[install.peer]
# Automatically install peer dependencies
autoInstallPeers = true
